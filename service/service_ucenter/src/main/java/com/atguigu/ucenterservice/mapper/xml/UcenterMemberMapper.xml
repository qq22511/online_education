<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.ucenterservice.mapper.UcenterMemberMapper">

    <select id="countRegister" resultType="java.lang.Integer" parameterType="string">
        Select COUNT(1) FROM ucenter_member where date(gmt_create) = #{day}
    </select>
</mapper>
